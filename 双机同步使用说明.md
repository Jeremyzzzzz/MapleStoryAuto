# 双机同步使用说明 (MapleStoryAuto 自动打猪)

本仓库 = 自动打猪脚本的版本管理仓库, 主要机器改完代码 `git push`, 另一台机器 `git pull` 即可同步。

## 仓库包含什么 (运行必需)

| 内容 | 路径 |
|---|---|
| 主脚本 | `tools/auto_combat.py` (约 7400 行, 含航点巡逻/打怪/挂机逻辑) |
| 感知/识别 | `tools/yolo_monster_viewer.py` (YOLO 怪物 + color_anchor 玩家 + 小地图红点) |
| 战斗辅助 | `tools/live_perception_viewer.py` (攻击时机判定/血量读取) |
| 配置 | `config/config_shanda_legacy.yaml` (麻超圆 攻击参数/航点参数/校准) |
| YOLO 权重 | `training_runs/wild_boar_real_hardneg_v4_960/weights/best.pt` (野猪, 5.3MB, 已入库) |
| 树妖模型 | `training_runs/warrior_stump_hardneg_v2_1280/weights/best.pt` (树妖/木妖, 5.2MB, 已入库; config `auto_combat.stump_model`) |
| 航点路线 | `minimaps/野猪的领土！！/waypoints.json` (当前录制路线) |
| 名字模板 | `nametag/麻超圆_*.png` 等 |
| 启动器 | `启动打猪.bat` (双击启动, 自动找 Python) |

> 注意: `minimaps/**/routes/` 历史路线存档、`log/`、`downloads/`、`training_runs/` 除 best.pt 外的内容
> 都在 `.gitignore` 里, **不会**上传; 本机这些目录属于运行产物。

## 第一台机器 (已配置好) 的日常操作

```
git add -A
git commit -m "说明"
git push origin main
```

## 另一台机器: 首次部署

1. **安装 Python**: 建议 3.12.x (与开发机一致; 3.10+ 均可)。安装时勾选 "Add to PATH"。
2. **拉代码** (装好 Git):
   ```
   git clone https://github.com/Jeremyzzzzz/MapleStoryAuto.git
   cd MapleStoryAuto
   ```
3. **装依赖** (PowerShell 或 cmd):
   ```
   pip install -r requirements.txt
   pip install torch torchvision ultralytics
   ```
   - 有 NVIDIA 显卡想快一点: `pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130`
   - 没有显卡/不想装 CUDA: 直接 `pip install torch torchvision ultralytics` (CPU 版也能跑, 帧率会低一些)
   - 如果要用 OCR(名字识别), 再装 `pip install rapidocr-onnxruntime`; 当前启动参数是 `--no-ocr` 不需要。
4. **游戏窗口设置**:
   - 游戏分辨率/窗口尺寸按 `config/config_shanda_legacy.yaml` 里的 `game_window: size: [750, 1278]` 设置 (游戏截图区域)。
   - 小地图画布校准 `minimap.canvas_region` (参考宽 1370) 是按这台机器标定的;
     两台机器通常同一套窗口设置就能直接用。**若坐标严重偏移** (面板对不齐/航点跑偏),
     每台机器自己校准一次: 主要看小地图左上角画布 `canvas_region` 和血条区域 `health_monitor.bar_regions`。
5. **双击 `启动打猪.bat`** 即可运行。
   - 它会依次找 `C:\quant_lab\lstm_gpu_venv\Scripts\python.exe` (本机开发环境) → `python` (PATH)。
   - 角色名 `麻超圆` 写在 bat 的 `--player-name` 参数里, 换角色请改这行。

## 另一台机器的日常更新

```
git pull
```
再双击 `启动打猪.bat` 即可。`启动打猪.bat` 会先清理残留进程/锁再启动, 与桌面版行为一致。

## 常用热键 (启动后, 先点一下游戏窗口聚焦)

- F1 开始录制 / F2 打普通点 / F3 打跳跃点
- F4 保存并开始巡航 / F5 清空录制 / F8 暂停恢复
- F6 点位置定位 / F10 安全点录制 / F11 恢复路线录制 / F12 恢复点位
- 安全点(F10): 按一次 F10 开始录制安全点 → 走位按 F2/F3 打点 → 到最后的商城位再按 F10 保存;
  巡游计时满 `safe_point.interval_seconds` 秒后, 等主航线【走完当前一圈(到最后一个巡游点)】
  才触发: 停止打怪走向安全点, 进商城(按 T)停留后 ESC+回车返回, 再恢复巡游(测谎仪规避)
- 恢复路线(F11): 从跌落底层的位置/商城出口开始, 走回巡游线, F2/F3 打点, 最后按 F11 保存;
  触发: ①安全点商城返回后自动走; ②巡游中跌落(玩家Y与恢复路线第一个点同水平)自动走;
  配置在 `recall_point` 段(trigger_y_tol / min_gap_y / cooldown_seconds)

## 开发约定

- 所有参数优先写进 `config/config_shanda_legacy.yaml` (带中文注释)。
- 改完代码必须同时更新 `启动打猪.bat` (如果启动参数变了) 并提交。
- 桌面快捷入口 `自动打猪.bat` 只是 wrapper, 实际内容在仓库 `启动打猪.bat`。
