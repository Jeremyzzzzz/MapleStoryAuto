# 交接文档 — 冒险岛自动打猪 bot（2026-08-26 回退版）

> 交接人：WorkBuddy（2026-08-26 16:30）
> 接手方：后续维护者 / codex
> 状态：**已回退到"发现框飘根因前"的稳定代码**，bot 已用回退版启动。

---

## 一、当前代码状态（重要）

**代码已回退**到 2026-08-26 16:06 我发"找到框飘的真正原因了"这句话**之前**的状态。
`tools/auto_combat.py` 和 `tools/yolo_monster_viewer.py` 均已恢复：

- ✅ `keep_color_anchor_misses=6`（yolo_monster_viewer.py，已从 30 改回）
- ✅ EMA 平滑只平滑**位置**、尺寸直取检测值（`_smoothed_box[2] = float(w)`）
- ✅ `_alpha = 0.85/0.35`（运动自适应，无 0.20 锚定分支）
- ✅ `_is_hold` 判定：`missed_frames>0 or PREDICTED or identity_mode=="color_anchor_hold"`（原版）
- ✅ 无"黄框锚定红框"逻辑、无尺寸平滑、无勋章红框可视化

**已移除的"锚定尝试"改动**（如果你之后想重新做，参考这个清单，别混进当前版）：
1. `keep_color_anchor_misses 6→30`
2. `_is_hold` 判定改为 `_imode != "color_anchor"`（把 local 也算 hold）
3. EMA 尺寸平滑（0.75/0.25）
4. 黄框锚定红框（`_anchored` + anchor_box 公式覆盖）
5. `_alpha = 0.20` 锚定强平滑
6. draw_detections 画"中级冒险家勋章"红框

备份：`/tmp/msbot_backup_20260826/auto_combat.py_anchor_attempt`（含锚定尝试的版本）。

---

## 二、用户的诉求（从未改变，务必先读）

1. **黄色玩家框必须稳定**，且**永远固定在红色勋章框（中级冒险家勋章检测框）上方的固定位置**。
   - 红框 = color_anchor 检测到的蓝色称号条（anchor_box，用户说它稳定）；
   - 黄框 = 玩家身体框，锚定关系：`玩家中心x = 称号条中心x`，`玩家中心y = 称号条y - color_anchor_name_offset_y(24) - center_offset_y(30)`（见 yolo_monster_viewer.py `_find_color_anchor` 1233-1236 行）。
2. **人物不动时黄框不能飘**。
3. 之前 codex 给过一版"检测称号框"的实现（红框），用户希望保留/加回这种可视化。
4. 用户已明确：**不再需要 WorkBuddy 继续改**，后续由 codex 接手。

---

## 三、已知问题（回退前观察到的）

### 问题 A：黄框飘（核心问题，未解决）
- **现象**（2026-08-26 16:05 日志）：角色不动（小地图 norm 恒 0.2541），但坐标在 266→277→279 跳、框宽 41~78 跳。
- **直接原因**：`检测=color_anchor_hold` / `local` 反复切换——color_anchor 频繁丢失（红分 0.19 接近阈值 0.02）→ 回退 local 模板匹配 → 又恢复。
- **注意**：回退后代码**仍会**出现这个现象（因为根因没修，只是恢复了原版行为）。回退的目的是把代码恢复到用户认可的稳定基线，问题交给 codex 重新分析。

### 问题 B：红分低（可能与 A 相关）
- 红分 0.19（之前稳定时 0.5+）。角色在跳跃对齐（wp_jump_align）时红分区域测不到红色翅膀。

### 问题 C：巡航掉层保护（已修复，保留）
- move 段掉层保护已改为**仅目标在上方（dy < -0.08）触发**——目标在下方（走下坡/掉下去）不触发。这个修复在回退中**保留**（它是 8-25 的修复，不是 8-26 锚定尝试的一部分）。

---

## 四、当前运行状态

- bot 已用**回退版代码**启动（`--show-viz` 可视化窗口开着）。
- 启动命令（桌面 `自动打猪.bat` v8，已改单斜杠路径）：
```
"C:\quant_lab\lstm_gpu_venv\Scripts\python.exe" tools\auto_combat.py --cfg shanda_legacy --monster-backend yolo --yolo-model training_runs\wild_boar_real_hardneg_v4_960\weights\best.pt --yolo-confidence 0.10 --yolo-iou 0.70 --yolo-image-size 960 --no-color-verify --show-viz --player-name 麻超圆 --fps-limit 12 --no-ocr --monster-labels wild_boar --no-terrain --mode minimap_patrol --map-name "野猪的领土！！"
```
- 日志：项目根 `bot_run.log`（stdout）或 `log/MSBot_*.log`（滚动文件）。
- 运行进程注意：`lstm_gpu_venv\Scripts\python.exe` 是**启动器**，会 spawn `codex-runtime` 的 python（内存 2-3GB 那个才是真身）。杀进程要杀真身（内存大的），锁文件在 `%TEMP%\auto_combat_ms.lock`（已从项目 log/ 目录移到 temp，防止旧锁残留）。

---

## 五、给接手方的建议

1. **先跑一版回退版观察**：确认"黄框飘"现象在回退版是否仍复现（预期会，因为根因未修）。
2. **黄框锚定红框的公式已确认**（`_find_color_anchor` 1233-1236 行），如果想做"黄框严格钉在红框上"，用这个公式，但**要解决红分低导致 color_anchor 频繁丢失的问题**（丢失时没有 anchor_box 可锚定）——这是核心矛盾。
3. 可能的根因方向：
   - 红分区域与跳跃姿态不匹配（跳起时身体不在固定偏移处）；
   - `identity_misses` / `keep_color_anchor_misses=6` 太短，频繁回退 local；
   - local 模板匹配的框本身不稳定（41~78 宽跳变）。
4. 用户偏好：中文界面/注释、参数可调（config yaml）、可视化红框排查误检、每次改动经真实游戏帧验证。

---

## 六、文件清单（相关）

| 文件 | 说明 |
|---|---|
| `tools/auto_combat.py` | 主脚本（已回退） |
| `tools/yolo_monster_viewer.py` | 玩家定位/怪物检测/小地图红点（已回退） |
| `config/config_shanda_legacy.yaml` | 配置（含 stump 树妖参数、红点参数） |
| `STARTUP.md` | 启动说明文档 |
| `Desktop\自动打猪.bat` | 桌面启动脚本 v8（单斜杠路径） |
| `/tmp/msbot_backup_20260826/` | 锚定尝试版本的备份 |
